programa {
  funcao inicio() 
  {

    real  a, b, c, d, g, f

    escreva(\ "Informe o publico do jogo=")
    leia(a)

   b= a * 0.1
   c= a * 0.5
   d= a * 0.3
   g= a * 0.1  
   
    
    f = (b * 5) + (c * 10) + (d * 20) + (g * 30)
    
    escreva("Valor total de arecadação é:", f)
      
  }
}
